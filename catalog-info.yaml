apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: {{name}}
  tags: [ai-agent]
  annotations:
    github.com/project-slug: {{repo}}
spec:
  type: service
  owner: {{owner}}
  lifecycle: experimental
